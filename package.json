{
  "name": "set-daily",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -k \"cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
